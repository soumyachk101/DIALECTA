{
  "name": "dialecta-extension",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "tsc -p tsconfig.json && node scripts/copy-static.mjs",
    "typecheck": "tsc -p tsconfig.json",
    "package": "npm run build"
  },
  "devDependencies": {
    "@types/chrome": "^0.0.287",
    "typescript": "^5.6"
  }
}
